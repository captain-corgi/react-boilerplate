/*
 * Skeleton Messages
 *
 * This contains all the text for the Skeleton container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Skeleton';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Skeleton container!',
  },
});
